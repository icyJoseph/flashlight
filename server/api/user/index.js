import { Router } from 'express';

const router = Router();

// Create user
router.get('/create', () => {});

// Get user by id
router.get('/:id', () => {});

// Get login screen
router.get('/login', () => {});
